spring:
  application:
    name: config-client
  profiles:
    active:
      development
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t

# By default, all sensitive endpoints added by actuator are secured. This includes ‘/refresh’ endpoint.
management.endpoints.web.exposure.include: "refresh"